Síncrono - Espera a tarefa anterior acabar para realizar a próxima 

Assíncrono - Não espera para realizar a próxima tarefa (Ex: setTimeout, Promises, Fetch, async etc.)

// Promises 


// Fetch api - isso é deveras importante então estudar pra valer 

// blob - importante já vi na molde.me 

// JSON - JavaScript Object Notation (JSON) é um formato de organização de dados, composto por um conjunto de chave e valor. 

{
    "id": 1, 
    "name": "erik", 
    "email": "erik@email"
}

// os dados de um json pode ser qualquer tipo (number, array, boolean, null etc)