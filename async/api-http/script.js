// async function puxarDados() {
//     const dadosRepsonse = await fetch("../json/dados.json")
//     const dadosText = await dadosRepsonse.json()
//     document.body.innerText = dadosText.map(name => name.name).join("\n")
// }

// puxarDados()

console.log(window.history)
