const agora = new Date()

agora.getDay(), // dia semana  
agora.getDate(), // dia numero 
agora.getMonth(), // mes 
agora.getFullYear(),
agora.getHours(),
agora.getMinutes(), 
agora.getTime(),
agora.getUTCHours()
